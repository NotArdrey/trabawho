# Admin Settings

`/admin/settings` will contain platform configuration that is appropriate for administrators. Settings require persistent labels, explanatory impact copy, typed validation, server-enforced permissions, and an audit record. High-risk changes require explicit confirmation and must preserve the previous value when saving fails.
